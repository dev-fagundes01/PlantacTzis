specVersion: "v1beta"
serviceId: "r.plantas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "r.plantas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
